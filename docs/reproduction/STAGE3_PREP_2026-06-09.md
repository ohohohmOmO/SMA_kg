# Stage 3 Preparation - 2026-06-09

This document records the repository state immediately before rerunning Stage 3
fusion and alignment after the Stage 1-2 hardening work.

Update: Stage 3 and Stage 4 were completed after this preparation document was
written. Current results are recorded in
`docs/reproduction/STAGE3_STAGE4_REPRO_2026-06-09.md`.

## Reference Documents

- `AGENTS.md`
- `docs/agents/PLAN.md`
- `docs/reproduction/ENGINEERING_HARDENING_2026-06-09.md`
- `docs/reproduction/STAGE2_FULL_LLM_EXTRACTION_2026-06-09.md`
- `.scratch/stage3-prep/PRD.md`
- `.scratch/stage3-prep/issues/01-review-topic-balanced-expansion.md`
- `.scratch/stage3-prep/issues/02-build-gold-set-before-medical-model-finetuning.md`

## Completed Stage 1 State

- Acquisition rerun succeeded in
  `artifacts/runs/stage1_data_acquisition_full_2026-06-09/`.
- Canonical PubMed input: `data/raw/pubmed_sma_abstracts.jsonl`
- Canonical PubMed records: 4554
- Canonical PubMed SHA-256:
  `5cfe801ab22312fa0cad317da994ff55ba96913df12a79c9464f472d2a622a48`
- Open Targets baseline records: 164
- Topic clustering rerun succeeded in
  `artifacts/runs/stage1_topic_clustering_full_2026-06-09/`.
- Canonical clustered abstracts: `data/processed/clustered_abstracts.jsonl`
- Clustered records: 4554
- Topic count: 67
- Topic representation uses `NeuML/pubmedbert-base-embeddings`,
  `ngram_range=(1, 3)`, `min_df=2`, `max_df=0.85`, and no static stop-word
  deletion.

## Open Stage 1 Decision

Topic-balanced PubMed retrieval produced 27 new candidate records in
`artifacts/runs/stage1_topic_balanced_pubmed_full_2026-06-09/outputs/data/raw/topic_balanced_pubmed_sma_abstracts.jsonl`.

These records are intentionally not promoted into
`data/raw/pubmed_sma_abstracts.jsonl`. The review and promotion decision is
tracked in `.scratch/stage3-prep/issues/01-review-topic-balanced-expansion.md`.

## Completed Stage 2 State

- Full Stage 2 LLM-only extraction succeeded in
  `artifacts/runs/stage2_extraction_llm_all_32w_2026-06-09/`.
- Model: `deepseek-ai/DeepSeek-V4-Flash`
- Input abstracts assigned to LLM: 4554
- Parallel workers: 32
- Chunk size: 5
- Raw LLM triples: `data/processed/llm_extracted_triples.jsonl`
- Raw LLM triple records: 18347
- Canonical Stage 2 triples: `data/processed/extracted_triples.jsonl`
- Canonical Stage 2 triple records: 18288
- Covered PMIDs: 3656
- Bad JSON lines: 0
- Invalid triples: 0
- Missing `evidence_text`: 0
- Missing `confidence_components`: 0
- Canonical SHA-256:
  `0d23d5dd162744dd70228905e6367800658e5d0af0b7328df50a6e62bfde76cb`

Stage 2 canonical output is LLM-only. Rule candidates are auxiliary and are not
merged into `data/processed/extracted_triples.jsonl` unless later verified by
an LLM or human reviewer.

## Open Stage 2 Decision

BioBERT/UIE-med fine tuning is not currently part of the production path. The
project first needs a 500-1000 item reviewed gold-standard set and a measured
baseline gap. This decision is tracked in
`.scratch/stage3-prep/issues/02-build-gold-set-before-medical-model-finetuning.md`.

## Stage 3 Readiness

Stage 3 should now consume:

- `data/processed/extracted_triples.jsonl`

Stage 3 should write a dated run directory such as:

- `artifacts/runs/stage3_fusion_full_2026-06-09/`

The run should record:

- consumed Stage 2 input path and SHA-256,
- dictionary and schema resource paths,
- alignment model,
- mapped, aligned, fused, and conflict output paths,
- validation summary,
- manifest,
- promotion status.

## Stale Artifacts To Replace

The following files predate the latest full Stage 2 extraction and must be
treated as stale only in the historical pre-run state captured by this document:

- `data/interim/mapped_triples.jsonl`
- `data/interim/aligned_triples.jsonl`
- `data/processed/fused_triples.jsonl`

They may be useful as historical comparison artifacts, but they are not the
current Stage 3 result for the full 2026-06-09 LLM-only Stage 2 output. They
were replaced by the promoted Stage 3 run documented in
`docs/reproduction/STAGE3_STAGE4_REPRO_2026-06-09.md`.

## Stage 3 Development Checklist

- Confirm `KG_SMA_env` is active or use the direct environment Python path.
- Read `docs/agents/PLAN.md` before running scripts.
- Use `data/processed/extracted_triples.jsonl` as the only Stage 2 canonical
  input unless the user explicitly promotes topic-balanced PubMed candidates and
  reruns Stage 2.
- Verify relation aliases and entity dictionaries load from `resources/`.
- Use the biomedical alignment model configured in `src/fusion/run_stage3_fusion.py`.
- Validate that relation conflict detection writes a reviewable output.
- Promote Stage 3 outputs only after validation passes.
